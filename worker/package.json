{
  "name": "whispr-worker",
  "description": "Whispr's single Cloudflare Worker — callAI proxy for OpenRouter",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "wrangler": "^4.0.0",
    "typescript": "^5.5.4",
    "@cloudflare/workers-types": "^4.20240909.0"
  }
}
