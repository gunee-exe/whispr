name = "whispr-callai"
main = "worker.js"
compatibility_date = "2024-01-01"

# Set the API key secret with:
#   wrangler secret put OPENROUTER_API_KEY
# Never commit your API key to source control.

[vars]
# Public non-secret vars go here (none needed for this worker).
